# macOS 万年历

## 目标
原生 macOS 14+，菜单栏与独立窗口，传统红色，农历、节气、完整黄历及联网更新中国大陆放假安排。以本次 ImageGen 设计为视觉基准，真实组件实现并截图对照。

## 进度
- [ ] ImageGen 生成并检查主窗口和菜单栏设计基准。
- [ ] 建立 Xcode 工程、设计 Token、历法与假期数据层。
- [ ] 实现主窗口、菜单栏面板、设置及生命周期。
- [x] 运行时 AppIcon 按系统当天日期动态绘制；静态默认图标固定为 12。
- [x] 静态及运行时 Dock 图标移除白色方形背景，保留透明画布。
- [x] 月历支持鼠标左右拖动与触控板双指横向滑动切换月份。
  - 2026-09-20：按反馈互换触控板双指方向映射；向左减一个月、向右加一个月，鼠标拖动方向保持不变。
- [x] 菜单栏面板支持点击外部、应用失焦和 Esc 收起。
- [x] 修复“今天”被调试固定日期覆盖后跳到 3 日的问题。
- [x] 通过 EventKit 读取并同步 Apple 日历事件，显示在“距离…”信息之前。
  - 验证：签名 Debug App 已启动；日历权限说明与 sandbox entitlement 均已写入；系统已授权并产生真实 EventKit 日期查询；完整单元测试 14 项通过；修正刷新通知回环后未再持续重复查询。
- [x] 菜单面板随日历事件数量增高，月历保持固定高度；超过 4 条时事件区域独立滚动。
  - 验证：0 至 20 条事件的高度模型回归测试通过；完整 16 项单元测试通过；签名 Debug App 构建并启动成功。
- [ ] 构建、测试、运行及设计 QA。
- [ ] 交付本地 app、设计图、证据和运行说明。
- [x] 接入 Sparkle：支持手动检查、自动检查和检查间隔设置。
  - 验证：Sparkle 2.10.0、Feed URL、公钥、Installer XPC、沙盒通信 entitlement 均进入 App；16 项单元测试通过。
- [x] 补充 README、初始化 Git、创建 GitHub 仓库并发布未公证 Release 包与 Appcast。
  - 验证：README、Release 脚本、1.0.0 ZIP 与 EdDSA Appcast 已完成；仓库已创建并推送，Release 发布后回读公开 URL。
- [x] 修复主窗口 10 月、12 月换行；菜单面板增加年份选择并统一优化年月选择器。
  - 验证：主窗口 10 月、12 月均为单行，菜单年份选择器可打开并定位 2026 年；针对性 UI 测试通过并留存截图附件。
- [x] 修复菜单面板切换日期后已同步的 Apple 日历事件被零高度区域裁掉的问题。
  - 验证：菜单尺寸刷新改用 `@Published` 回调的新事件快照；事件高度状态转换回归测试及完整 17 项单元测试通过。
- [x] 为项目补充 MIT License，并在 README 中声明项目许可证。
- [x] 软件更新检查间隔仅保留“从不、每天、每周”；“从不”关闭主动检查但保留手动检查。
  - 验证：旧的每小时/每 6 小时设置会归一化为每天；18 项单元测试和设置窗口专项 UI 测试通过。
- [x] 构建并发布未公证的 1.0.1（Build 2），推送 main、版本标签、Sparkle Appcast 和 GitHub Release 安装包。
  - 验证：通用架构 ZIP 解压后通过深度签名校验且不含 `get-task-allow`；Release 资产下载返回 200，大小 24,552,460 字节，SHA-256 为 `e779c912580dc2d376f0dd861038df383aadb450940bafc8bb67c62d35687c06`；远端 Appcast 已回读 1.0.1。
- [x] 关闭主窗口后切换为菜单栏常驻模式并隐藏 Dock 图标；从菜单栏重新打开窗口时恢复 Dock。
  - 验证：窗口展示策略单元测试覆盖主窗口、设置窗口和纯菜单栏三种状态；19 项单元测试通过，Debug App 构建并启动成功（PID 41366）。专项 UI 测试已补充，但本机 Xcode 自动化服务启用超时，未完成实际点击验证。
- [ ] 构建并发布未公证的 1.0.2（Build 3），推送 main、版本标签、Sparkle Appcast 和 GitHub Release 安装包。

## 约束
- 当前目录不是 Git 仓库；遵循用户要求，不自行初始化，不推送或发布。
- 用户目标为原生 macOS，Product Design 网页模板和浏览器验收改用原生组件及运行截图；保留视觉对照检查。
- 截图数据仅作视觉参考，历法及假期使用实际数据，不硬编码展示日期。

## 验证
- 动态／静态图标：1、3、12、31 日渲染及非法日期边界通过；实际导出 3 日、12 日图检查通过。
- 构建与启动：`./script/build_and_run.sh --verify` 通过；产物 Info.plist 已包含 `AppIcon`。
- 日历与假期数据：8 项测试通过。
- 本轮交互：新增手势识别 4 项，连同既有测试共 12 项全部通过；`./script/build_and_run.sh --verify` 构建启动通过。
- 菜单面板：实际显示时检测到 layer 25 面板窗口；切换 Finder 后该窗口消失，应用失焦收起通过。点击外部与 Esc 的事件路径已编译，受自动化输入权限限制未做系统级模拟点击验收。
- Dock 图标透明度：基础图、静态数字 12 和 1024 AppIcon 均为非全不透明图，四角 alpha=0；动态渲染角点 alpha 单元测试及 Debug 构建启动通过。当前环境不能读取显示器画面，未取得 Dock 实景截图。
- “今天”修复：测试固定日期改为显式 `--fixed-today YYYY-MM-DD`，旧的 `--fixed-day 3` 不再生效；普通启动只读系统日期。新增从 3 日返回系统 19 日的回归测试，连同既有测试共 13 项通过，Debug 构建启动通过。
