# 万年历设计基准

来源：design/calendar-reference.png（内置 ImageGen，提示词见 design/prompt.txt）。
原生 SwiftUI / AppKit 内容，禁止将整张设计图铺为界面。

## Token
- 主色：朱红 #CF3039；选中底色 #D12936；节假日底色 #FCEEEF。
- 正文 #171C24；次级文字 #646A73；邻月文字 #AFB0B4。
- 页面底色 #FCFAF8；卡片 #F2F0ED；分隔线 #E3DFDC。
- 宜：#21855A；补班：#355572。深色对应值见 DesignTokens.swift。
- 系统中文 / SF 数字，农历标题使用系统宋体 Songti SC；图标使用 SF Symbols。
- 主窗口基准内容 1036×876，左列 65.4%，右列 34.6%，原生系统标题栏。
- 左列水平 28、顶部 26、区块间距 22；日期格圆角 14；卡片圆角 15。
- 菜单栏面板宽 380、高 660，内边距 18，日期格圆角 10。按用户 2026-09-19 的位置与样式反馈压紧面板；小屏按所在屏幕可用高度缩短日期网格。

## 保真边界
- 生成图的日期重复、漏字、干支与黄历事实以真实历法替换。
- 原生系统标题栏、菜单、弹出箭头使用系统实现，不描画模拟系统控件。
- 宜忌保持单行摘要，点击后弹出完整条目；其他黄历详情允许滚动。
- 图片不含独立插画或照片资产，故不额外生成无关装饰素材。

## AppIcon
- 安装包静态默认图标固定显示 12；应用启动后替换为当天日号。
- 动态底图：`Resources/Icon/calendar-base.png`，运行时使用 Baskerville SemiBold 和朱红色绘制 `1...31`。
- 动态图标覆盖 Dock、应用切换器和应用运行期间的图标；Finder 中未运行的 `.app` 使用静态 AppIcon。
